const service = require('../../src/services/confirmationService');

const testProductResponse = "";
test("should return proper confirmation component data for one product", () => {
    service.prepareConfirmationData("clientId", {})

});